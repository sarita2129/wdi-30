import React, { Component } from 'react'; // hehehe it's really preact
import ReactDOM from 'react-dom'; // hehehe it's really preact
import 'preact/devtools';

import App from './components/App';

ReactDOM.render(<App />, document.body);
